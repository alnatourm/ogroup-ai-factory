# Design trigger files

A commit under this directory triggers the Stitch Design Agent workflow on main. The workflow reads the canonical design request from `factory/design-requests/arabic-ai-ipaas.v0.1.json` unless manually dispatched with another request path.
