{
  "name": "@ogroup/arabic-ai-ipaas-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit --module NodeNext --moduleResolution NodeNext --target ES2022 --strict --skipLibCheck src/*.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/railway-server.js",
    "migrate": "node dist/migrate.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "supertest": "^7.1.0",
    "@types/supertest": "^6.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@types/pg": "^8.11.10"
  }
}
